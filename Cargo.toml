[package]
name = "janus"
version = "0.1.1"
edition = "2024"

[dependencies]
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
jsonschema = { version = "0.46.5", default-features = false }
